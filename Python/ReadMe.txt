ExactChange - Breaks down a figure to provide the exact number of each coin to give back as change.

ExactChange2 - Another way of solving the same problem.

List-Word-Freq - Takes a list of words from a file and reports what words there are and how many times it appears.

Message Encrypting - Take an encryption key and message from the user and outputs the message in it's encrypted form.

Number Guessing Game - User has to guess a random number chosen from a range such as 1-10.

Seasons - Takes a date and tells the user if it's valid or not and what season it would be.

SortingDicList - Takes a file of words listed and sorts them.

TextBasedGame - Self explanatory, Explore "Rooms" and pick up items to defeat the final boss.